/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsListAttestations } from "../../funcs/bankAccountsListAttestations.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListBankAccountAttestationsRequest$inboundSchema,
};

export const tool$bankAccountsListAttestations: ToolDefinition<typeof args> = {
  name: "bank-accounts-list-attestations",
  description: `List the attestations submitted for a bank account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bankAccountsListAttestations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
