/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Contains the intermediate signing key for verification.
 */
export type GooglePayIntermediateSigningKey = {
  /**
   * A JSON-encoded serialized string containing the signing key data.
   */
  signedKey: string;
  /**
   * Base64-encoded signatures of the `signedKey`.
   */
  signatures: Array<string>;
};

/** @internal */
export const GooglePayIntermediateSigningKey$inboundSchema: z.ZodType<
  GooglePayIntermediateSigningKey,
  z.ZodTypeDef,
  unknown
> = z.object({
  signedKey: z.string(),
  signatures: z.array(z.string()),
});
/** @internal */
export type GooglePayIntermediateSigningKey$Outbound = {
  signedKey: string;
  signatures: Array<string>;
};

/** @internal */
export const GooglePayIntermediateSigningKey$outboundSchema: z.ZodType<
  GooglePayIntermediateSigningKey$Outbound,
  z.ZodTypeDef,
  GooglePayIntermediateSigningKey
> = z.object({
  signedKey: z.string(),
  signatures: z.array(z.string()),
});

export function googlePayIntermediateSigningKeyToJSON(
  googlePayIntermediateSigningKey: GooglePayIntermediateSigningKey,
): string {
  return JSON.stringify(
    GooglePayIntermediateSigningKey$outboundSchema.parse(
      googlePayIntermediateSigningKey,
    ),
  );
}
export function googlePayIntermediateSigningKeyFromJSON(
  jsonString: string,
): SafeParseResult<GooglePayIntermediateSigningKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GooglePayIntermediateSigningKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GooglePayIntermediateSigningKey' from JSON`,
  );
}
