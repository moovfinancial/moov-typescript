/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  IssuingIntervalLimit,
  IssuingIntervalLimit$inboundSchema,
  IssuingIntervalLimit$outboundSchema,
} from "./issuingintervallimit.js";

/**
 * A velocity limit with its current runtime state.
 */
export type IssuedVelocityLimit = {
  /**
   * The maximum amount in cents that can be spent in a given interval.
   */
  amount?: number | undefined;
  /**
   * The maximum number of transactions allowed in the given interval.
   */
  count?: number | undefined;
  /**
   * Specifies the time frame for a velocity limit. `per-transaction` applies to each individual authorization and never resets. Time-based intervals (where supported) reset at midnight ET.
   */
  interval: IssuingIntervalLimit;
  /**
   * The amount in cents already spent in the current interval.
   */
  amountUsed?: number | undefined;
  /**
   * The amount in cents remaining in the current interval.
   */
  amountRemaining?: number | undefined;
  /**
   * The number of transactions already made in the current interval.
   */
  countUsed?: number | undefined;
  /**
   * The number of transactions remaining in the current interval.
   */
  countRemaining?: number | undefined;
  /**
   * When the current interval resets. Absent for per-transaction limits.
   */
  resetsOn?: Date | undefined;
};

/** @internal */
export const IssuedVelocityLimit$inboundSchema: z.ZodType<
  IssuedVelocityLimit,
  z.ZodTypeDef,
  unknown
> = z.object({
  amount: types.optional(types.number()),
  count: types.optional(types.number()),
  interval: IssuingIntervalLimit$inboundSchema,
  amountUsed: types.optional(types.number()),
  amountRemaining: types.optional(types.number()),
  countUsed: types.optional(types.number()),
  countRemaining: types.optional(types.number()),
  resetsOn: types.optional(types.date()),
});
/** @internal */
export type IssuedVelocityLimit$Outbound = {
  amount?: number | undefined;
  count?: number | undefined;
  interval: string;
  amountUsed?: number | undefined;
  amountRemaining?: number | undefined;
  countUsed?: number | undefined;
  countRemaining?: number | undefined;
  resetsOn?: string | undefined;
};

/** @internal */
export const IssuedVelocityLimit$outboundSchema: z.ZodType<
  IssuedVelocityLimit$Outbound,
  z.ZodTypeDef,
  IssuedVelocityLimit
> = z.object({
  amount: z.number().int().optional(),
  count: z.number().int().optional(),
  interval: IssuingIntervalLimit$outboundSchema,
  amountUsed: z.number().int().optional(),
  amountRemaining: z.number().int().optional(),
  countUsed: z.number().int().optional(),
  countRemaining: z.number().int().optional(),
  resetsOn: z.date().transform(v => v.toISOString()).optional(),
});

export function issuedVelocityLimitToJSON(
  issuedVelocityLimit: IssuedVelocityLimit,
): string {
  return JSON.stringify(
    IssuedVelocityLimit$outboundSchema.parse(issuedVelocityLimit),
  );
}
export function issuedVelocityLimitFromJSON(
  jsonString: string,
): SafeParseResult<IssuedVelocityLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IssuedVelocityLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IssuedVelocityLimit' from JSON`,
  );
}
