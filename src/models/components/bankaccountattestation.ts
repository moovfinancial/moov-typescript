/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * An attestation that a new authorization was obtained for a R29-errored bank account.
 */
export type BankAccountAttestation = {
  /**
   * ID of the created attestation.
   */
  attestationID: string;
  /**
   * ID of the bank account the attestation belongs to.
   */
  bankAccountID: string;
  /**
   * ID of the Moov account the bank account is linked to.
   */
  accountID: string;
  /**
   * Timestamp at which the attestation was created.
   */
  createdOn: Date;
  /**
   * Date on which new authorization was obtained from the receiver, formatted as YYYY-MM-DD.
   */
  attestedAt: Date;
  /**
   * Freeform text description describing how the authorization was obtained.
   */
  description: string;
};

/** @internal */
export const BankAccountAttestation$inboundSchema: z.ZodType<
  BankAccountAttestation,
  z.ZodTypeDef,
  unknown
> = z.object({
  attestationID: types.string(),
  bankAccountID: types.string(),
  accountID: types.string(),
  createdOn: types.date(),
  attestedAt: types.date(),
  description: types.string(),
});
/** @internal */
export type BankAccountAttestation$Outbound = {
  attestationID: string;
  bankAccountID: string;
  accountID: string;
  createdOn: string;
  attestedAt: string;
  description: string;
};

/** @internal */
export const BankAccountAttestation$outboundSchema: z.ZodType<
  BankAccountAttestation$Outbound,
  z.ZodTypeDef,
  BankAccountAttestation
> = z.object({
  attestationID: z.string(),
  bankAccountID: z.string(),
  accountID: z.string(),
  createdOn: z.date().transform(v => v.toISOString()),
  attestedAt: z.date().transform(v =>
    v.toISOString().slice(0, "YYYY-MM-DD".length)
  ),
  description: z.string(),
});

export function bankAccountAttestationToJSON(
  bankAccountAttestation: BankAccountAttestation,
): string {
  return JSON.stringify(
    BankAccountAttestation$outboundSchema.parse(bankAccountAttestation),
  );
}
export function bankAccountAttestationFromJSON(
  jsonString: string,
): SafeParseResult<BankAccountAttestation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BankAccountAttestation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BankAccountAttestation' from JSON`,
  );
}
