/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  IssuingScheduleDay,
  IssuingScheduleDay$inboundSchema,
  IssuingScheduleDay$outboundSchema,
} from "./issuingscheduleday.js";

/**
 * A window of time during which the card may authorize.
 */
export type ScheduleWindow = {
  /**
   * The days of the week this window applies to.
   */
  days: Array<IssuingScheduleDay>;
  /**
   * Inclusive window start time in 24-hour `HH:MM` format.
   */
  startTime: string;
  /**
   * Exclusive window end time in 24-hour `HH:MM` format. If earlier than `startTime`, the window wraps past midnight.
   */
  endTime: string;
};

/** @internal */
export const ScheduleWindow$inboundSchema: z.ZodType<
  ScheduleWindow,
  z.ZodTypeDef,
  unknown
> = z.object({
  days: z.array(IssuingScheduleDay$inboundSchema),
  startTime: types.string(),
  endTime: types.string(),
});
/** @internal */
export type ScheduleWindow$Outbound = {
  days: Array<string>;
  startTime: string;
  endTime: string;
};

/** @internal */
export const ScheduleWindow$outboundSchema: z.ZodType<
  ScheduleWindow$Outbound,
  z.ZodTypeDef,
  ScheduleWindow
> = z.object({
  days: z.array(IssuingScheduleDay$outboundSchema),
  startTime: z.string(),
  endTime: z.string(),
});

export function scheduleWindowToJSON(scheduleWindow: ScheduleWindow): string {
  return JSON.stringify(ScheduleWindow$outboundSchema.parse(scheduleWindow));
}
export function scheduleWindowFromJSON(
  jsonString: string,
): SafeParseResult<ScheduleWindow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduleWindow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduleWindow' from JSON`,
  );
}
