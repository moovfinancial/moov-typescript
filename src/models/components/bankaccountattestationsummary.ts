/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A partner-visible summary of a bank account attestation.
 */
export type BankAccountAttestationSummary = {
  /**
   * ID of the attestation.
   */
  attestationID: string;
  /**
   * Freeform text description describing how the authorization was obtained.
   */
  description: string;
  /**
   * Timestamp at which the attestation was created.
   */
  createdOn: Date;
  /**
   * Date on which new authorization was obtained from the receiver, formatted as YYYY-MM-DD.
   */
  attestedAt: Date;
};

/** @internal */
export const BankAccountAttestationSummary$inboundSchema: z.ZodType<
  BankAccountAttestationSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  attestationID: types.string(),
  description: types.string(),
  createdOn: types.date(),
  attestedAt: types.date(),
});
/** @internal */
export type BankAccountAttestationSummary$Outbound = {
  attestationID: string;
  description: string;
  createdOn: string;
  attestedAt: string;
};

/** @internal */
export const BankAccountAttestationSummary$outboundSchema: z.ZodType<
  BankAccountAttestationSummary$Outbound,
  z.ZodTypeDef,
  BankAccountAttestationSummary
> = z.object({
  attestationID: z.string(),
  description: z.string(),
  createdOn: z.date().transform(v => v.toISOString()),
  attestedAt: z.date().transform(v =>
    v.toISOString().slice(0, "YYYY-MM-DD".length)
  ),
});

export function bankAccountAttestationSummaryToJSON(
  bankAccountAttestationSummary: BankAccountAttestationSummary,
): string {
  return JSON.stringify(
    BankAccountAttestationSummary$outboundSchema.parse(
      bankAccountAttestationSummary,
    ),
  );
}
export function bankAccountAttestationSummaryFromJSON(
  jsonString: string,
): SafeParseResult<BankAccountAttestationSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BankAccountAttestationSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BankAccountAttestationSummary' from JSON`,
  );
}
