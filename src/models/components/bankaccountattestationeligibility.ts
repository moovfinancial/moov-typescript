/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Whether a bank account is currently eligible to receive a new attestation.
 */
export type BankAccountAttestationEligibility = {
  /**
   * Whether the calling account is allowlisted for the attestations feature. When `false`, `eligible` is always `false`.
   */
  enabled: boolean;
  /**
   * Whether the bank account currently meets the requirements to receive a new attestation.
   */
  eligible: boolean;
};

/** @internal */
export const BankAccountAttestationEligibility$inboundSchema: z.ZodType<
  BankAccountAttestationEligibility,
  z.ZodTypeDef,
  unknown
> = z.object({
  enabled: types.boolean(),
  eligible: types.boolean(),
});
/** @internal */
export type BankAccountAttestationEligibility$Outbound = {
  enabled: boolean;
  eligible: boolean;
};

/** @internal */
export const BankAccountAttestationEligibility$outboundSchema: z.ZodType<
  BankAccountAttestationEligibility$Outbound,
  z.ZodTypeDef,
  BankAccountAttestationEligibility
> = z.object({
  enabled: z.boolean(),
  eligible: z.boolean(),
});

export function bankAccountAttestationEligibilityToJSON(
  bankAccountAttestationEligibility: BankAccountAttestationEligibility,
): string {
  return JSON.stringify(
    BankAccountAttestationEligibility$outboundSchema.parse(
      bankAccountAttestationEligibility,
    ),
  );
}
export function bankAccountAttestationEligibilityFromJSON(
  jsonString: string,
): SafeParseResult<BankAccountAttestationEligibility, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BankAccountAttestationEligibility$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BankAccountAttestationEligibility' from JSON`,
  );
}
